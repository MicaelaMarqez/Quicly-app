import React from "react"
import { Text, View, StyleSheet, ImageBackground, Image,TouchableOpacity,ScrollView} from "react-native"

const Profile = () => {
	return(
		<View>
			<Text>Esta es la Profile</Text>
		</View>
	)
}

export default Profile

const styles = StyleSheet.create({
  });